# Моя робота v9.1 Stable

Виправлено порожній календар на iPhone.

Причина: Safari/PWA міг одночасно використати модулі різних версій.
Новий календар звертався до `dayNotes`, яких не було у старому модулі стану.

Зроблено:
- унікальна версія кожного JS-файлу: v9-1-stable-20260803;
- видалення старих кешів;
- мережеве оновлення перед використанням кешу;
- безпечне читання старих локальних даних;
- резервні значення для змін, планів, налаштувань і нотаток;
- старі записи не видаляються.
